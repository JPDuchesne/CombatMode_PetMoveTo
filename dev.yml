name: combatmode-petmoveto
lua: "5.1"

commands:
  up:
    desc: Project-specific setup (luarocks init)
    run: ./bin/up
  test:
    desc: Run unit tests
    run: ./bin/test
  package:
    desc: Build release artifact
    run: ./bin/package
  release:
    desc: Tag and release
    run: ./bin/release
