name: combatmode-petmoveto
lua: "5.1"

commands:
  test:
    desc: Run unit tests
    run: ./bin/test
  package:
    desc: Build release artifact
    run: ./bin/package
  release:
    desc: Tag and release
    run: ./bin/release
